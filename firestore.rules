rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Workshops collection: Allow reading and managing workshop tenant data
    match /workshops/{workshopId} {
      allow read, write: if true;
    }

    // Default fallback
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
